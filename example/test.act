if: (x && y = 10) {
    x + 1; print: x;
    y + 1; print: y;
}

## comments are cool.

test: (x * y) {
    returns: (%result > 15) {
        x + 1; print:x;
        y + 1; print:y;
    }
    returns: (%result < 15){
        var: new = x + y; 
        print: new;
    }
}

func: multiplyAndDivide(a,b) {
    var: new = %x * %y;
    
}